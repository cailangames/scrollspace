// Code for playing sound effects

#ifndef _SOUND_EFFECTS_H_
#define _SOUND_EFFECTS_H_

// Mutes all the sound channels.
void mute_all_channels(void);

// Plays all the sound channels.
void play_all_channels(void);

// Plays the mode selection sound effect.
void play_mode_selection_sound(void);

// Plays the fly away sound effect.
void play_flyaway_sound(void);

// Plays the gun sound effect.
void play_gun_sound(void);

// Plays the bomb sound effect.
void play_bomb_sound(void);

// Plays the player collision sound effect.
void play_collision_sound(void);

// Plays the health sound effect.
void play_health_sound(void);

// Plays the shield sound effect.
void play_shield_sound(void);

// Plays the game over sound effect.
void play_gameover_sound(void);

#endif
